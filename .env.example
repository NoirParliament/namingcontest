# Copy this file to `.env` and fill in the values. `.env` is gitignored.
# Both are safe to expose to the browser (RLS protects the data, not the key).

# Supabase → Project Settings → API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
